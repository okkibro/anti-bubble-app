image:
stages:
  - build
  - test

cache:
  paths:
    - node_modules/

Build:
  stage: build
  script:
    - npm install

Test:
  stage: test
  script: 
    - ./node_modules/@angular/bin/ng test --watch=false --browsers=ChromeHeadless