stages:
  - build
  - test

cache:
  paths:
    - node_modules/

install_dependencies:
  stage: build
  script:
    - npm install -g @angular/cli
    - npm install
    - npm audit fix

testing_testing:
  stage: test
  script: npm test